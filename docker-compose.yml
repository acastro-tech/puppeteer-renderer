services:
  puppeteer-renderer:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
    deploy:
      resources:
        limits:
          memory: 1g
